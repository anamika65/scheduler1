/**
 * Jquery for Add resident Depending on the User_Role selected
 */

$(document).ready(
function() {
	$("input,select,textarea").not("[type=submit]").jqBootstrapValidation(); 
	$("#zebraTable tr:nth-child(even)").addClass("zerba");
});
